from kismet_scorecard import *
import random as ran
import os

seed_size = 42 # number of random bits to pull from the random number generator
# seed the random number generator
ran.seed(os.urandom(seed_size))



